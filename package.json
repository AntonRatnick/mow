{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^12.0.1",
    "@commitlint/config-conventional": "^12.0.1",
    "husky": "^5.1.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "prepare": "husky install"
  }
}
